# 🧮 CI/CD Test Calculator

Простой калькулятор для изучения процессов Continuous Integration и Continuous Deployment.

## 📋 Описание проекта

Этот проект создан специально для изучения CI/CD процессов. Он содержит простое CLI приложение-калькулятор с полным набором автоматизированных проверок.

## 🚀 Как использовать

### 1. Клонирование и настройка

```bash
# Форкните этот репозиторий в свой GitHub аккаунт
git clone https://github.com/ваш-username/ci-test.git
cd ci-test

# Установите зависимости
pip install -r requirements.txt
```

### 2. Использование калькулятора

```bash
# Сложение
python calculator.py add 5 3

# Вычитание
python calculator.py sub 10 4

# Умножение
python calculator.py mul 6 7

# Деление
python calculator.py div 15 3

# Возведение в степень
python calculator.py pow 2 8
```

### 3. Запуск тестов локально

```bash
# Все тесты
pytest

# Тесты с покрытием
pytest --cov=calculator

# Проверка стиля кода
flake8 calculator.py test_calculator.py

# Проверка форматирования
black --check calculator.py test_calculator.py

# Проверка безопасности
bandit calculator.py
```

## 🔍 CI/CD Pipeline

После пуша в репозиторий автоматически запускаются следующие проверки:

1. **Code Linting** - проверка стиля кода (flake8, isort)
2. **Code Formatting** - проверка форматирования (black)  
3. **Type Checking** - проверка типов (mypy)
4. **Security Check** - проверка безопасности (bandit)
5. **Unit Tests** - запуск unit тестов (pytest)
6. **Integration Test** - интеграционные тесты

## 🎯 Задания для изучения

### Задание 1: Сломать линтер

Внесите следующие изменения в `calculator.py`:

```python
# Плохой стиль - добавьте лишние пробелы и длинные строки
def add(a,b):    # убрать пробелы вокруг запятой
    return a+b   # убрать пробелы вокруг +

# Или добавьте очень длинную строку (больше 88 символов)
def very_long_function_name_that_exceeds_line_length_limit_and_will_fail_linting_check():
    pass
```

**Результат**: CI упадёт на этапе `Code Linting`

**Как исправить**: 
```bash
black calculator.py  # автоматическое форматирование
flake8 calculator.py # проверка оставшихся проблем
```

### Задание 2: Сломать тесты

Внесите ошибку в логику калькулятора:

```python
def add(a, b):
    """Сложение двух чисел."""
    return a - b  # ОШИБКА: изменили + на -
```

Или добавьте намеренную ошибку:

```python
def divide(a, b):
    """Деление двух чисел."""
    return a / b  # убрать проверку на деление на ноль
```

**Результат**: CI упадёт на этапе `Unit Tests`

**Как исправить**: Верните правильную логику и проверки

## 🛠️ Полезные команды для восстановления

```bash
# Автоматическое форматирование
black calculator.py test_calculator.py

# Автоматическая сортировка импортов  
isort calculator.py test_calculator.py

# Проверка всех инструментов разом
flake8 calculator.py test_calculator.py && \
black --check calculator.py test_calculator.py && \
pytest -v
```

## 📊 Мониторинг CI

1. Перейдите в раздел **Actions** вашего GitHub репозитория
2. Наблюдайте за выполнением pipeline
3. Изучайте логи каждого этапа
4. Анализируйте причины падения тестов

## 🎓 Цели обучения

После выполнения заданий вы:
- Поймёте как работает CI/CD pipeline
- Научитесь читать логи автоматизированных проверок
- Освоите инструменты качества кода (linters, formatters)
- Поймёте важность автоматизированного тестирования
- Научитесь исправлять проблемы на основе результатов CI

## 📁 Структура проекта

```
ci-test/
├── calculator.py           # Основное приложение
├── test_calculator.py      # Unit тесты
├── requirements.txt        # Зависимости
├── pyproject.toml         # Конфигурация проекта
├── .flake8               # Настройки линтера
├── .github/workflows/    # GitHub Actions
│   └── ci.yml           # CI pipeline
└── README.md            # Документация
```

Happy learning! 🚀 